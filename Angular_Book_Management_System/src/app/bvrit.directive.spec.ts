import { BvritDirective } from './bvrit.directive';

describe('BvritDirective', () => {
  it('should create an instance', () => {
    const directive = new BvritDirective();
    expect(directive).toBeTruthy();
  });
});
